# 画布模型目录配置详细说明

## 概述

"系统管理-模型与路由-画布模型目录"是用于配置画布客户端可同步的模型目录。画布客户端会定期从服务器同步这个目录，用户在画布中选择模型时会看到这里配置的模型列表。

---

## 核心字段说明

### 1. Remote ID（必填）
**字段名**: `remote_id`

**作用**: 
- 对应上游 API 实际使用的模型标识
- 这是计费时使用的模型名称，必须与渠道中配置的模型名称完全一致
- 例如：`sd5-seedance-2.0`、`gpt-4o`、`claude-3-5-sonnet`

**填写规则**:
- 必须与中转站（渠道）中添加的模型名称一致
- 不能重复（每个 remote_id 只能配置一次）
- 从"未配置"列表打开时会自动锁定，无法修改

**示例**:
```
sd5-seedance-2.0
flux-1.1-pro
gpt-4o
claude-3-5-sonnet-20241022
```

---

### 2. 显示名称（必填）
**字段名**: `display_name`

**作用**: 
- 在画布客户端中显示给用户看的友好名称
- 可以使用中文或更易读的描述

**填写规则**:
- 不能为空
- 建议使用易懂的名称，方便用户识别

**示例**:
```
seedance 2.0 满血
Flux 1.1 Pro
GPT-4o
Claude 3.5 Sonnet
```

---

### 3. 能力（Capabilities）⭐
**字段名**: `capabilities`

**作用**: 
- 声明这个模型具备什么能力
- 系统会根据能力自动推导出对应的 contract
- 多个能力用逗号分隔

**当前支持的能力**:
| 能力标识 | 说明 | 对应 Contract |
|---------|------|--------------|
| `video_gen` | 视频生成能力 | `relay_video_async_v1` |
| `image_gen` | 图片生成能力 | `relay_image_async_v1` |

**填写规则**:
- 可以填写多个能力，用逗号、顿号、分号或空格分隔
- 例如：`video_gen`、`image_gen`、`video_gen,VideoGen`
- 大小写敏感，但系统会自动识别常见变体
- 新建时，系统会根据能力自动填充 contract 字段

**示例**:
```
video_gen
image_gen
video_gen,VideoGen
```

**工作原理**:
```
能力输入: video_gen
         ↓ (自动推导)
Contract: relay_video_async_v1
```

---

### 4. Contract（必填）⭐
**字段名**: `contract`

**作用**: 
- 指定画布客户端使用的协议模板 ID
- 画布客户端内置了不同的协议模板来处理不同类型的 API 调用
- **如果 contract 填错，画布客户端会跳过这条目录，用户完全看不到这个模型**

**当前支持的 Contract**:
| Contract 名称 | 用途 | 支持的能力 |
|--------------|-----|----------|
| `relay_video_async_v1` | 异步视频生成协议 | video_gen |
| `relay_image_async_v1` | 异步图片生成协议 | image_gen |

**填写规则**:
- 必填项，不能为空
- 必须从下拉列表中选择受支持的 contract
- 只能选择画布客户端已内置的协议模板
- 如果能力字段已填写，系统会自动推导出正确的 contract

**自动推导机制**:
- **新建模型时**：输入能力后，系统会自动填充对应的 contract
- **编辑模型时**：不会自动覆盖，保留原有配置
- **手动修改后**：系统不再自动推导，防止覆盖手动设置

**客户端支持率显示**:
- 界面会显示当前在线画布客户端对该 contract 的支持情况
- 例如："12/15 个在线客户端支持"表示有 12 个客户端支持该协议
- 如果显示 "0/N"，说明没有任何客户端支持，**不应该上架这个模型**

**错误处理**:
```
正确 contract → 画布正常显示和使用
错误 contract → 画布跳过该条目（用户看不到）
未知 contract → 画布跳过该条目（用户看不到）
```

---

## 能力与 Contract 的关系

### 映射关系图
```
┌──────────────┐         ┌─────────────────────────┐
│  能力输入     │  自动   │   Contract 输出          │
│ (Capability) │  推导   │                         │
├──────────────┤  ───→   ├─────────────────────────┤
│ video_gen    │         │ relay_video_async_v1    │
│ image_gen    │         │ relay_image_async_v1    │
└──────────────┘         └─────────────────────────┘
```

### 工作流程

#### 场景 1：新建模型（推荐流程）
1. 填写 Remote ID：`sd5-seedance-2.0`
2. 填写显示名称：`seedance 2.0 满血`
3. **填写能力**：`video_gen`
4. **系统自动推导并填充 Contract**：`relay_video_async_v1`
5. 检查客户端支持率：确认有足够的客户端支持
6. 保存

#### 场景 2：编辑现有模型
1. 打开编辑对话框
2. Contract 保持原有值，**不会被自动覆盖**
3. 如需更改 contract，手动从下拉列表选择
4. 保存

#### 场景 3：手动设置（逃生舱）
1. 填写能力：`video_gen`
2. 系统自动填充 contract
3. **手动更改 contract**：选择其他值
4. 此后系统不再自动推导，保留手动设置

---

## 其他重要字段

### 5. 启用开关
**字段名**: `enabled`

**作用**: 
- 控制模型的上架状态
- 停用后画布仍保留该模型记录，但用户不可再发起新生成（软下线）
- 已发起的任务不受影响

**状态说明**:
- ✅ **启用**：用户可以在画布中看到并使用该模型
- ❌ **停用**：画布显示该模型但置灰，不可选择（软下线）

### 6. 说明
**字段名**: `description`

**作用**: 
- 展示给用户的模型说明文字
- **只读字段**，在画布目录页面不能编辑

**编辑方式**:
- 需要在"模型管理-元信息"页面编辑
- 说明统一维护，画布客户端与定价页均使用这份说明
- 点击"去模型管理页编辑"链接可直接跳转

### 7. 价格
**字段名**: `pricing`

**作用**: 
- 展示给画布用户的价格文案
- 可以自定义价格说明

**填写规则**:
- **留空时**：客户端按分组价格自动展示
- **填写时**：使用自定义文案，例如 `2.8元/条`、`$0.05/张`
- 会显示"当前计费(自动文案)"作为参考

### 8. 限制说明
**字段名**: `limitations`

**作用**: 
- 向用户说明该模型的使用限制
- 例如：分辨率限制、时长限制、并发限制等

**示例**:
```
最大分辨率：1920x1080
视频时长：5-10秒
不支持音频
```

---

## 高级设置

### 9. 词汇表要求
**字段名**: `requires_vocab`

**作用**: 
- 控制是否需要词汇表支持
- 默认值：`1`（需要）

**选项**:
- `1`：需要词汇表（默认）
- `0`：不需要词汇表

### 10. 排序顺序
**字段名**: `sort_order`

**作用**: 
- 控制模型在画布列表中的显示顺序
- 数字越小，排序越靠前
- 默认值：`0`

**示例**:
```
sort_order: 0  → 最优先显示
sort_order: 10 → 第二优先
sort_order: 20 → 第三优先
```

### 11. 参数 Schema
**字段名**: `param_schema`

**作用**: 
- 定义该模型接受的参数结构（JSON Schema 格式）
- 画布客户端根据这个 schema 生成参数输入界面

**填写规则**:
- 必须是合法的 JSON 格式
- 留空时使用默认 schema

### 12. Schema 覆盖
**字段名**: `schema_override`

**作用**: 
- 覆盖画布客户端内置的参数词汇表
- 用于自定义参数名称、默认值、范围等

**填写规则**:
- 必须是合法的 JSON 格式
- 填写错误会被画布逐条跳过，不影响其他条目
- 真正的词汇表校验在画布 Rust 端执行

**示例**:
```json
{
  "resolution": {
    "default": "1024x1024",
    "options": ["512x512", "1024x1024", "2048x2048"]
  }
}
```

---

## 配置流程最佳实践

### 步骤 1：确认渠道已配置
在配置画布目录前，确保：
1. 渠道中已添加该模型
2. 模型名称（remote_id）已知
3. 模型已启用并可用

### 步骤 2：创建目录条目
1. 点击"新增目录条目"
2. 填写 **Remote ID**（与渠道中的模型名称一致）
3. 填写 **显示名称**（用户友好的名称）
4. 填写 **能力**（例如 `video_gen`）
5. 系统自动填充 **Contract**
6. 检查**客户端支持率**（确保 > 0）

### 步骤 3：配置价格和说明
1. 在"模型管理-元信息"中添加说明
2. 在画布目录中配置价格文案（可选）

### 步骤 4：测试和上架
1. 保存配置
2. 在画布客户端同步目录
3. 确认模型出现在列表中
4. 测试生成功能
5. 启用上架

---

## 常见问题

### Q1: 为什么我配置的模型在画布中看不到？
**可能原因**:
1. ❌ Contract 填错或未选择受支持的 contract
2. ❌ 显示名称为空
3. ❌ 模型被停用（enabled = false）
4. ❌ 客户端版本过旧，不支持该 contract

**解决方法**:
- 检查 contract 是否在支持列表中
- 查看客户端支持率是否为 0
- 确认显示名称已填写
- 确认模型处于启用状态

### Q2: Contract 应该选择哪个？
**答案**:
- **视频生成模型**：选择 `relay_video_async_v1`
- **图片生成模型**：选择 `relay_image_async_v1`
- **不确定时**：先填写能力字段，让系统自动推导

### Q3: 能力和 Contract 有什么区别？
**答案**:
- **能力（Capabilities）**: 人类可读的模型能力描述（`video_gen`、`image_gen`）
- **Contract**: 画布客户端实际使用的协议模板 ID（`relay_video_async_v1`）
- 系统会根据能力自动推导 contract，通常不需要手动设置

### Q4: 客户端支持率是什么意思？
**答案**:
- 显示当前在线的画布客户端中有多少支持该 contract
- 例如："12/15 个在线客户端支持"表示 15 个客户端中有 12 个支持
- **如果显示 0/N，不要上架该模型**，因为没有任何客户端能使用

### Q5: 为什么能力字段不会自动推导 contract 了？
**答案**:
- **编辑现有模型时**：系统不会覆盖已有的 contract 配置
- **手动修改 contract 后**：系统视为"逃生舱"，不再自动推导
- **新建模型时**：正常自动推导

### Q6: Remote ID 可以修改吗？
**答案**:
- **编辑模式**：可以修改
- **从"未配置"列表打开时**：自动锁定，不能修改（因为已经关联了具体模型）

### Q7: 说明字段为什么不能编辑？
**答案**:
- 说明统一在"模型管理-元信息"页面维护（统一真源）
- 画布目录、定价页、API 文档都使用同一份说明
- 点击"去模型管理页编辑"可跳转编辑

---

## 技术细节

### 数据库字段映射
```go
type CanvasCatalogModel struct {
    Id             int    `json:"id"`
    RemoteID       string `json:"remote_id"`       // 模型标识
    DisplayName    string `json:"display_name"`    // 显示名称
    Capabilities   string `json:"capabilities"`    // 能力（逗号分隔）
    Enabled        *bool  `json:"enabled"`         // 启用状态
    Contract       string `json:"contract"`        // 协议模板 ID
    Pricing        string `json:"pricing"`         // 价格文案
    Limitations    string `json:"limitations"`     // 限制说明
    ParamSchema    string `json:"param_schema"`    // 参数 Schema
    SchemaOverride string `json:"schema_override"` // Schema 覆盖
    RequiresVocab  int    `json:"requires_vocab"`  // 词汇表要求
    SortOrder      int    `json:"sort_order"`      // 排序
}
```

### Contract 支持检测
```go
// 当前支持的 Contract
var capabilityToContract = map[string]string{
    "video_gen": "relay_video_async_v1",
    "image_gen": "relay_image_async_v1",
}

// 检查 Contract 是否受支持
func IsSupportedContract(contract string) bool {
    // 返回 true 表示画布客户端支持该 contract
    // 返回 false 表示未知 contract，画布会跳过该条目
}
```

### 目录同步机制
1. 画布客户端定期调用 `/api/canvas_catalog` 获取目录
2. 服务器返回所有条目（包括停用的）
3. 客户端根据 `enabled` 和 `contract` 过滤可用模型
4. 未知 contract 的条目会被跳过

---

## 总结

### 配置检查清单
创建或编辑画布目录条目时，请确认：

- ✅ Remote ID 与渠道中的模型名称一致
- ✅ 显示名称已填写且友好易读
- ✅ 能力字段已填写（video_gen 或 image_gen）
- ✅ Contract 已选择且受客户端支持（支持率 > 0）
- ✅ 模型已启用（enabled = true）
- ✅ 说明已在"模型管理"中添加
- ✅ 价格文案已配置（可选）

### 核心要点
1. **能力自动推导 Contract**：填写能力后，系统会自动选择正确的 contract
2. **Contract 决定可用性**：填错 contract 会导致模型在画布中不可见
3. **客户端支持率**：上架前必须确认有客户端支持（> 0）
4. **说明统一维护**：在"模型管理"页面编辑，多处共享
5. **软下线机制**：停用后用户仍能看到模型但不可使用

遵循这些规则，可以确保模型正确配置并在画布客户端中正常显示和使用。
